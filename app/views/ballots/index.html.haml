%h1 All Votes
-poll = Poll.last
.row
  .col-xs-12
    =link_to 'previous', ballots_path, id: "change_graph"
    .chart
      -poll_presenter = PollPresenter.new(poll)
      = pie_chart poll_presenter.piechart_data
      = poll_presenter.number_of_votes
%table.table.table-striped
  %tbody
    %tr
      %td
        Keg
      %td
        Voter
    -@ballots.each do |ballot|
      %tr
        %td
          #{ ballot.beer.brand }
        %td
          #{ ballot.user.username }
        %td
          =link_to "change", edit_ballot_path(ballot)
        %td
          =link_to "delete", ballot_path(ballot), method: :delete, data: { confirm: "Are you sure you want to delete you ballot?" }

.action-links
  = link_to "Back", beers_path, class: "btn btn-lg btn-default pull-right"
