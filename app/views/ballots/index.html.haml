%h1 Poll details
-poll = Poll.last
.row
  .col-xs-12
    =link_to 'Previous poll', ballots_path, class: "chart-link previous", "data-poll_id" => poll.previous.id
    =link_to 'Next poll', ballots_path, class: "chart-link next utility-link", "data-poll_id" => Poll.first.id
    .chart
      -poll_presenter = PollPresenter.new(poll)
      = pie_chart poll_presenter.piechart_data
      = poll_presenter.number_of_votes
%table.table.table-striped
  %tbody
    %tr
      %th
        Keg
      %th
        Voter
      %th
      %th
    -poll.ballots.each do |ballot|
      %tr
        %td
          #{ ballot.beer.brand }
        %td
          #{ ballot.user.username }
        %td
          =link_to "Edit", edit_ballot_path(ballot)
        %td
          =link_to "Delete", ballot_path(ballot), method: :delete, data: { confirm: "Are you sure you want to delete you ballot?" }
