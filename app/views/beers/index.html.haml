%h1 Available Beers
%table
  %tbody
    %tr
      Brands:
    -@beers.each do |beer|
      %tr
        %td
          #{ beer.brand }
        %td
          =link_to "Delete", beer_path(beer), method: :delete, data: { confirm: "Are you sure you want to delete this beer?" }
        %td
          =link_to "Edit", edit_beer_path(beer)
        %td
          =link_to "Voote!", ballots_path(beer_id: beer.id), method: :post
=link_to "Add Beer", new_beer_path
